version: '2'
services:
  elasticsearch:
    image: elasticsearch:5
    networks:
      - atrix-elasticsearch_test
    ports:
      - 9200:9200

networks:
  atrix-elasticsearch_test:
    driver: bridge
